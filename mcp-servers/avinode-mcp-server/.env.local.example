# Avinode MCP Server - Environment Variables Example
# 
# IMPORTANT SECURITY NOTES:
# - This file contains PLACEHOLDER values only - NEVER use real credentials here
# - Copy this file to .env.local and fill in your actual credentials
# - Store real secrets in secure vaults or environment variables, never in git
# - If you have committed real credentials, revoke/rotate them immediately
# - Use git-filter-repo or BFG to remove secrets from git history before pushing
#
# Required environment variables for Avinode API integration

# Avinode API Base URL (optional - defaults to sandbox)
# Use https://sandbox.avinode.com/api for testing
# Use https://api.avinode.com/api for production
BASE_URI=https://sandbox.avinode.com/api

# Avinode API Token (required)
# This is the X-Avinode-ApiToken header value
# Get this from your Avinode Developer Portal dashboard
API_TOKEN=XXX_CHANGE_ME_API_TOKEN_PLACEHOLDER

# Avinode Bearer/Authentication Token (required)
# This is used in the Authorization: Bearer header
# Get this from your Avinode Developer Portal dashboard
AUTHENTICATION_TOKEN=XXX_CHANGE_ME_BEARER_TOKEN_PLACEHOLDER

# Alternative environment variable names (for backwards compatibility)
# These are checked if the primary names above are not set
# AVINODE_BASE_URL=https://sandbox.avinode.com/api
# AVINODE_API_TOKEN=XXX_CHANGE_ME_API_TOKEN_PLACEHOLDER
# AVINODE_BEARER_TOKEN=XXX_CHANGE_ME_BEARER_TOKEN_PLACEHOLDER