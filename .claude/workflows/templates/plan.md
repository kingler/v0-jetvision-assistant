---
phase: {phase-number}
plan: {plan-number}
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "{Observable behavior users can verify}"
  artifacts:
    - "{File path that must exist after completion}"
  key_links:
    - "{Critical connection: component -> API -> database}"
---

# Implementation Plan: {Plan Title}

> Generated by `/dev-framework plan`

<objective>
{What this plan accomplishes, why it matters, what artifacts it creates.
Keep to 2-3 sentences.}
</objective>

<context>
- Spec: {path to spec.md}
- Stack: Next.js 14, TypeScript, Supabase, Tailwind CSS
- Existing patterns: {relevant patterns from codebase}
</context>

<tasks>

<task type="auto">
  <name>{Action-oriented name — verb first}</name>
  <files>{path/to/file1.ts, path/to/file2.ts}</files>
  <action>
    {Precise implementation instructions.
    Include what to create/modify and what to avoid.
    Reference existing patterns where applicable.}
  </action>
  <verify>{Command or check proving completion — e.g., npm run test:unit}</verify>
  <done>{Acceptance criteria — measurable state when task is complete}</done>
</task>

<task type="auto">
  <name>{Next task}</name>
  <files>{path/to/file.ts}</files>
  <action>{Instructions}</action>
  <verify>{Check}</verify>
  <done>{Criteria}</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>{What was just implemented}</what-built>
  <how-to-verify>{Steps for user to verify — e.g., visit URL, click button, check output}</how-to-verify>
  <expected>{What the user should see}</expected>
</task>

</tasks>

<verification>
After all tasks complete:
1. {Overall check 1}
2. {Overall check 2}
3. Run: npm run test:unit
4. Run: npm run lint
</verification>

<success_criteria>
- {Measurable outcome from spec}
- {Another measurable outcome}
</success_criteria>
